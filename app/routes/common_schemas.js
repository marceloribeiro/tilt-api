/**
 * @swagger
 * components:
 *   schemas:
 *     # ... (previous schemas remain the same) ...
 *
 *     User:
 *       type: object
 *       properties:
 *         id:
 *           type: integer
 *         phone_number:
 *           type: string
 *         email:
 *           type: string
 *         user_name:
 *           type: string
 *         first_name:
 *           type: string
 *         last_name:
 *           type: string
 *         created_at:
 *           type: string
 *           format: date-time
 *         updated_at:
 *           type: string
 *           format: date-time
 *
 *     Error:
 *       type: object
 *       properties:
 *         message:
 *           type: string
 */